<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsTestProject Condition="'$(RunIntegrationTests)' != 'true'">false</IsTestProject>
    <IsTestingPlatformApplication Condition="'$(RunIntegrationTests)' != 'true'">false</IsTestingPlatformApplication>
    <OutputType Condition="'$(RunIntegrationTests)' != 'true'">Library</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.IdentityModel.JsonWebTokens" />
    <PackageReference Include="Olve.Results.TUnit" />
    <PackageReference Include="Testcontainers" />
    <PackageReference Include="TUnit" />
    <PackageReference Include="Refit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../clients/Olve.SlimeRepublics.Client/Olve.SlimeRepublics.Client.csproj" />
  </ItemGroup>

</Project>
